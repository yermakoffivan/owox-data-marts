{
  "name": "owox",
  "description": "OWOX Data Marts CLI: Simple command-line interface to start the OWOX Data Marts application with backend and frontend components.",
  "version": "0.32.0",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "owox": "./bin/run.js"
  },
  "bugs": "https://github.com/OWOX/owox-data-marts/issues",
  "dependencies": {
    "@oclif/core": "^4",
    "@oclif/plugin-help": "^6",
    "@owox/backend": "*",
    "@owox/idp-better-auth": "*",
    "@owox/idp-owox-better-auth": "*",
    "@owox/idp-protocol": "*",
    "@owox/internal-helpers": "*",
    "@owox/web": "*",
    "cors": "^2.8.6"
  },
  "peerDependencies": {
    "@nestjs/platform-express": "^11",
    "express": "^5",
    "zod": "^3.25"
  },
  "devDependencies": {
    "@eslint/compat": "^1",
    "@oclif/test": "^4",
    "@owox/prettier-config": "*",
    "@types/chai": "^4",
    "@types/cors": "^2.8.19",
    "@types/express": "^5",
    "@types/mocha": "^10",
    "@types/node": "^18",
    "@types/supertest": "^6.0.2",
    "chai": "^4",
    "eslint-config-oclif": "6.0.146",
    "eslint-config-prettier": "~10.1.0",
    "mocha": "^11",
    "oclif": "^4",
    "supertest": "^7.0.0",
    "ts-node": "^10"
  },
  "engines": {
    "node": ">=22.22.0"
  },
  "files": ["./bin", "./dist/**/*.js", "./oclif.manifest.json", "./npm-shrinkwrap.json"],
  "homepage": "https://github.com/OWOX/owox-data-marts",
  "keywords": [
    "data-marts",
    "cli",
    "server",
    "nestjs",
    "backend",
    "owox",
    "data-orchestration",
    "sqlite",
    "mysql",
    "bigquery",
    "athena"
  ],
  "license": "ELv2",
  "type": "module",
  "oclif": {
    "bin": "owox",
    "dirname": "owox",
    "commands": "./dist/commands",
    "plugins": ["@oclif/plugin-help"],
    "topicSeparator": " "
  },
  "repository": "OWOX/owox-data-marts",
  "scripts": {
    "build": "tsc",
    "build:backend": "npm run build -w @owox/backend --prefix ../..",
    "build:clean": "shx rm -rf dist oclif.manifest.json tsconfig.tsbuildinfo",
    "build:dep": "npm run build:internal-helpers && npm run build:web && npm run build:idp-protocol && npm run build:idp-better-auth && npm run build:idp-owox-better-auth && npm run build:backend",
    "build:web": "npm run build -w @owox/web --prefix ../..",
    "build:idp-protocol": "npm run build -w @owox/idp-protocol --prefix ../..",
    "build:idp-better-auth": "npm run build -w @owox/idp-better-auth --prefix ../..",
    "build:idp-owox-better-auth": "npm run build -w @owox/idp-owox-better-auth --prefix ../..",
    "build:internal-helpers": "npm run build -w @owox/internal-helpers --prefix ../..",
    "dev": "cross-env OWOX_TELEMETRY_DISABLED=1 node ./bin/run.js serve -e ../../.env",
    "lint": "eslint . --config ./eslint.config.mjs",
    "lint:fix": "eslint . --config ./eslint.config.mjs --fix",
    "lint:md": "markdownlint-cli2 --config ../../.markdownlint-cli2.mjs",
    "lint:md:fix": "markdownlint-cli2 --config ../../.markdownlint-cli2.mjs --fix",
    "migrations:up": "node ./bin/run.js migrations up -e ../../.env",
    "migrations:down": "node ./bin/run.js migrations down -e ../../.env",
    "migrations:status": "node ./bin/run.js migrations status -e ../../.env",
    "format": "prettier --write \"**/*.{ts,js,json}\" --ignore-path ../../.prettierignore",
    "format:check": "prettier --check \"**/*.{ts,js,json}\" --ignore-path ../../.prettierignore",
    "prebuild": "npm run build:clean && npm run build:dep",
    "prepack": "oclif manifest",
    "prepublishOnly": "npm run lint",
    "pretest": "oclif manifest",
    "test": "mocha --forbid-only \"test/**/*.test.ts\"",
    "version": "oclif readme && git add README.md"
  }
}
